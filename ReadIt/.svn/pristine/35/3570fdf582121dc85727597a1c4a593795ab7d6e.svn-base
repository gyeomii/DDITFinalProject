<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Point-Mapper">

	<select id="selectPointListByMember" resultType="point">
		select * 
		from point
		where mem_id = #{mem_id}
	</select>
</mapper>